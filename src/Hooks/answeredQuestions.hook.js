
export function useAnsweredQuestions () {
    
    function addQuestion ({id, question, items, validAnswer, userAnswer}) {
        
    }
    
    return {
        addQuestion,
        getAnsweredList,
    }
}